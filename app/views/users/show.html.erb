<!DOCTYPE html>
<html>
<head>
  <title>Portfólio de <%= @user.name %></title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
</head>
<body>
  <div class="container mt-5">
    <h1><%= @user.name %></h1>
    <p>QA Analyst | Cypress | Postman | Bancos de dados SQL | NodeJs | Ruby | React | NextJS | Azure | </p>
    <p><%= @user.email %></p>

    <h2>Sobre</h2>
      <div class="card mb-3">
        <div class="card-body bg-secondary-subtle">
          <p>Profissional com mais de 10 anos de experiência na área de Tecnologia da Informação, com expertise em análise, desenvolvimento e testes de software. Capaz de trabalhar eficientemente em equipe, contribuindo para a melhoria contínua dos processos de qualidade e garantindo a entrega de soluções robustas e funcionais.</p>
        </div>       
      </div>

    <h2>Experiências</h2>
    <% @user.experiences.each do |experience| %>
      <div class="card mb-3">
        <div class="card-body bg-secondary-subtle">
          <h5 class="card-title"><%= experience.title %> - <%= experience.company %></h5>
          <h6 class="card-subtitle mb-2 text-muted"><%= experience.start_date %> - <%= experience.end_date %></h6>
          <p class="card-text"><%= experience.description %></p>
        </div>
      </div>
    <% end %>

    <h2>Educação</h2>
    <% @user.educations.each do |education| %>
      <div class="card mb-3">
        <div class="card-body bg-secondary-subtle">
          <h5 class="card-title"><%= education.degree %> - <%= education.school %></h5>
          <h6 class="card-subtitle mb-2 text-muted"><%= education.start_date %> - <%= education.end_date %></h6>
          <p class="card-text"><%= education.description %></p>
        </div>
      </div>
    <% end %>

    <h2>Habilidades</h2>
    <% @user.skills.each do |skill| %>
      <div class="card mb-3">
        <div class="card-body bg-secondary-subtle">
          <p class="card-text"><%= skill.name %> - <%= skill.proficiency %></p>
        </div>
      </div>
    <% end %>
  </div>
</body>
</html>
